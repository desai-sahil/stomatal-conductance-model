numpy>=1.21
scipy>=1.7
pandas>=1.3
matplotlib>=3.5
# Pin panel + bokeh to a known-good pair (per DEPLOYMENT.md troubleshooting note).
# These are required by `panel convert --to pyodide-worker` in the GitHub Action.
bokeh==3.6.*
panel==1.5.*
jupyter>=1.0
